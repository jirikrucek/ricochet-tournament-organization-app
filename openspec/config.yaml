schema: spec-driven

context: |
  Read files `agent-os/product/mission.md`, `agent-os/product/roadmap.md`, `agent-os/product/tech-stack.md` to identify key information about this project and codebase.
  After reading, say "I have read the files and understand the project and codebase."

rules:
  design: |
    Follow instructions in `.github/prompts/agent-os/agent-os.inject-standards.prompt.md` to inject standards into design.
    After injecting standards, say "I have injected standards into design."